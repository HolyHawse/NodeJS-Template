<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= typeof title !== 'undefined' ? title : 'NodeJS Project' %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <% if (typeof customCss !== 'undefined') { %>
    <link rel="stylesheet" href="<%= customCss %>">
  <% } %>
  <style>
    body { background: #f8f9fa; transition: background 0.3s, color 0.3s; }
    .navbar-brand { cursor: pointer; }
    body.dark-mode { background: #181a1b !important; color: #e2e2e2; }
    body.dark-mode .bg-white, body.dark-mode .card { background: #23272b !important; color: #e2e2e2; }
    body.dark-mode .navbar { background: #23272b !important; }
    body.dark-mode .dropdown-menu { background: #23272b; color: #e2e2e2; }
    body.dark-mode .dropdown-item { color: #e2e2e2; }
    body.dark-mode .dropdown-item:hover { background: #343a40; color: #fff; }
    body.dark-mode .btn-primary { background: #375a7f; border-color: #375a7f; }
    body.dark-mode .alert-info { background: #23272b; color: #e2e2e2; border-color: #375a7f; }
    body.dark-mode footer { color: #aaa; }
    body.dark-mode .form-control, 
    body.dark-mode textarea.form-control, 
    body.dark-mode input.form-control, 
    body.dark-mode select.form-control {
      background-color: #23272b !important;
      color: #e2e2e2 !important;
      border-color: #444 !important;
    }
    body.dark-mode .form-control:focus {
      background-color: #23272b !important;
      color: #fff !important;
      border-color: #888 !important;
    }
    body.dark-mode .text-muted {
      color: #b0b8c1 !important;
    }
      body.dark-mode .modal-content {
    background-color: #23272b !important;
    color: #e2e2e2 !important;
    border-color: #444 !important;
  }
  body.dark-mode .modal-header,
  body.dark-mode .modal-footer {
    background-color: #23272b !important;
    color: #e2e2e2 !important;
    border-color: #444 !important;
  }
  body.dark-mode .modal-title {
    color: #e2e2e2 !important;
  }
  body.dark-mode .btn-close {
    filter: invert(1) grayscale(1);
  }
  body.dark-mode .form-select {
    background-color: #23272b !important;
    color: #e2e2e2 !important;
    border-color: #444 !important;
  }
  body.dark-mode .form-select:focus {
    background-color: #23272b !important;
    color: #fff !important;
    border-color: #888 !important;
  }
  </style>
  <% if (typeof customHead !== 'undefined') { %>
    <%- customHead %>
  <% } %>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
      <div class="dropdown">
        <a class="navbar-brand dropdown-toggle" href="#" id="logoDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          NodeJS Project
        </a>
        <ul class="dropdown-menu" aria-labelledby="logoDropdown">
          <li><a class="dropdown-item <%= activePage === 'main' ? 'active' : '' %>" href="/">Ana Sayfa</a></li>
          <% if (user) { %>
            <li><a class="dropdown-item <%= activePage === 'profile' ? 'active' : '' %>" href="/profile">Profilim</a></li>
            <% if (user.isAdmin) { %>
              <li><a class="dropdown-item <%= activePage === 'dashboard' ? 'active' : '' %>" href="/dashboard">Dashboard</a></li>
              <li><a class="dropdown-item <%= activePage === 'users' ? 'active' : '' %>" href="/users">Kullanıcılar</a></li>
              <li><a class="dropdown-item <%= activePage === 'sql' ? 'active' : '' %>" href="/sql">SQL</a></li>
            <% } %>
          <% } %>
          <li><a class="dropdown-item <%= activePage === 'contact' ? 'active' : '' %>" href="/contact">İletişim</a></li>
          <li><a class="dropdown-item <%= activePage === 'information' ? 'active' : '' %>" href="/information">Hakkında</a></li>
        </ul>
      </div>
      <div class="d-flex align-items-center ms-auto">
        <button id="themeToggle" class="btn btn-outline-light btn-sm me-2" title="Gündüz/Gece Modu">
          <span id="themeIcon" class="bi bi-moon"></span>
        </button>
        <% if (user) { %>
          <span class="username text-white"><%= user.name %> <%= user.surname %></span>
          <a href="/logout" class="btn btn-light btn-sm ms-2">Çıkış Yap</a>
        <% } else { %>
          <a href="/login" class="btn btn-light btn-sm">Giriş Yap</a>
        <% } %>
      </div>
    </div>
  </nav>
  <main>
    <%- body %>
  </main>
  <footer class="text-center mt-4">
    <small>&copy; 2025 HolyHawse</small>
  </footer>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    function setTheme(dark) {
      if (dark) {
        document.body.classList.add('dark-mode');
        document.getElementById('themeIcon').className = 'bi bi-sun';
      } else {
        document.body.classList.remove('dark-mode');
        document.getElementById('themeIcon').className = 'bi bi-moon';
      }
    }
    function getTheme() {
      return localStorage.getItem('theme') === 'dark';
    }
    document.addEventListener('DOMContentLoaded', function() {
      setTheme(getTheme());
      document.getElementById('themeToggle').addEventListener('click', function() {
        const dark = !getTheme();
        localStorage.setItem('theme', dark ? 'dark' : 'light');
        setTheme(dark);
      });
    });
  </script>
  <% if (typeof customScript !== 'undefined') { %>
    <%- customScript %>
  <% } %>
</body>
</html>