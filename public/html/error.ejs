<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>403 - Yetkisiz Erişim</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
      body { background: #f8f9fa; }
      .centered-403 {
        min-height: 80vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
      }
      .error-code {
        font-size: 5rem;
        font-weight: bold;
        color: #dc3545;
      }
      .error-message {
        font-size: 1.3rem;
        color: #555;
        margin-bottom: 24px;
      }
      .theme-btn {
        position: fixed;
        top: 18px;
        right: 18px;
        z-index: 1000;
      }
      body.dark-mode {
        background: #181a1b !important;
        color: #e2e2e2;
      }
      body.dark-mode .error-code { color: #ff6b81; }
      body.dark-mode .error-message { color: #e2e2e2; }
      body.dark-mode .btn-primary { background: #375a7f; border-color: #375a7f; }
    </style>
</head>
<body>
  <button id="themeToggle" class="btn btn-outline-secondary theme-btn" title="Aydınlık/Karanlık Mod">
    <span id="themeIcon" class="bi bi-moon"></span>
  </button>
  <div class="centered-403">
    <div class="error-code">403</div>
    <div class="error-message">
      Bu sayfaya erişim yetkiniz yok.
    </div>
    <a href="/" class="btn btn-primary"><i class="bi bi-house"></i> Ana Sayfa</a>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    function setTheme(dark) {
      if (dark) {
        document.body.classList.add('dark-mode');
        document.getElementById('themeIcon').className = 'bi bi-sun';
      } else {
        document.body.classList.remove('dark-mode');
        document.getElementById('themeIcon').className = 'bi bi-moon';
      }
    }
    function getTheme() {
      return localStorage.getItem('theme') === 'dark';
    }
    document.addEventListener('DOMContentLoaded', function() {
      setTheme(getTheme());
      document.getElementById('themeToggle').addEventListener('click', function() {
        const dark = !getTheme();
        localStorage.setItem('theme', dark ? 'dark' : 'light');
        setTheme(dark);
      });
    });
  </script>
</body>
</html>